/** @format */
import TransactionForm from "@/components/TransactionForm";

export default function Transaction() {
  return (
    <main className="w-full flex items-center justify-center flex-col min-h-dvh">
      <TransactionForm />
    </main>
  );
}
